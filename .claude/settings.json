{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in *vendor/noir_rs/*) ;; *.rs) rustfmt --edition 2021 \"$f\" ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
