[package]
name = "outbe_circuit_core"
type = "lib"
authors = ["outbe"]
compiler_version = ">=1.0.0"

[dependencies]
# Poseidon2 (owner commitment + binding pre-hash + Merkle nodes) uses the
# stdlib `std::hash::poseidon2_permutation`, so no external poseidon dep.
